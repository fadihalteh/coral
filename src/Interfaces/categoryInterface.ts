export interface Category {
    id: number;
    name: string;
    icon: string;
    image: string;
    image_mobile: string;
    createdAt: string;
    updatedAt: string;
  }

  export interface ErrorResponse {
    error: string;
  }
  